apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minikube-ingress
  annotations:
spec:
  rules:
  - host: hello.world
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: web-service
            port:
              number: 5000
      - path: /cities
        pathType: ImplementationSpecific
        backend:
          service:
            name: web-service
            port:
              number: 5000
  
